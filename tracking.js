// Запись событий воронки в таблицу event_tracking (общая с основным магазином).
// События: chatbot_opened, chatbot_message_sent, chatbot_recommendation_given,
//          chatbot_to_cart, chatbot_to_pricelookup, chatbot_to_manager, chatbot_offtop_blocked.

// TODO: Этап 4 — реализовать track(eventType, data)

export async function track(_eventType, _data = {}) {
  // заглушка
}
